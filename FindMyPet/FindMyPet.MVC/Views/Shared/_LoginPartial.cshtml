@using Microsoft.AspNet.Identity

@if (Request.IsAuthenticated)
{
    string ownerName = Session["OwnerName"] as string;
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
    {
    @Html.AntiForgeryToken()

    <ul class="navbar-nav ml-auto">
        <li class="nav-item">
            <a id="manage" href="/Manage" class="nav-link" title="Manage">
                <i class="icon-user g-mr-5"></i>@ownerName
            </a>
        </li>
        <li class="nav-item">
            <a id="logoff" href="javascript:document.getElementById('logoutForm').submit()" class="nav-link">Log off</a>
        </li>
    </ul>
    }
}
else
{
    <ul class="navbar-nav ml-auto">
        <li class="nav-item">
            @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { @class = "nav-link", id = "registerLink" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { @class = "nav-link", id = "loginLink" })
        </li>
    </ul>
}
